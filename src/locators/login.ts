import {test} from '@playwright/test';

export const LoginLocators = {
    username:   '#username',
    password:   '#password',
    checkbox:   'input[type="checkbox"]',
    signInBtn:  '#signInBtn'
};